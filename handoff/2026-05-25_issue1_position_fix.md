# Handoff — Issue #1 クリック領域 位置ズレ補正

- **日付**: 2026-05-25
- **発行**: Claude Code (L.A.M.I.A / 現場監督・目)
- **実装担当**: Codex (手)
- **対象**: `index.html` 先頭の `STORES` 配列の `x,y`（map.png 977×1339 上の 0–100%）
- **方法**: 現状座標を `map.png` にオーバーレイ描画し、店名ラベルとのズレを実測（`?debug=1` 相当をオフライン再現）→ 明確にズレている店のみ補正値を確定。中央列(16–27)・右列(27–34, 44–46)・上段の大半は整合のため変更なし。

## 確定した補正（この5件のみ・他は触らない）

| # | 店名 | 旧 (x,y) | 新 (x,y) | 理由 |
|---|------|----------|----------|------|
| 2 | 東北電力研究開発センター | 54.0, 7.8 | **51.0, 9.5** | マーカーが店名テキストより上にズレ → 下げて文字列に乗せる |
| 40 | 和泉生花 | 28.7, 59.4 | **37.0, 61.0** | 左に大きくズレ（柏屋/たけむらや列より左すぎ）→ 右へ |
| 41 | 西原歯科医院 | 45.0, 74.6 | **48.0, 76.0** | わずかに左上 → 右下へ |
| 42 | 認定こども園 TOBINOKO | 25.6, 78.7 | **32.0, 80.5** | 左にズレ＋園ラベルより上 → 右下へ |
| 43 | 多賀多夢舎中山工房 | 23.5, 83.6 | **34.0, 82.0** | 左に大きくズレ → 右へ |

## Codex への実装指示

1. `index.html` の `STORES` 配列で上記5店の `x,y` のみを新値に書き換える。**他の42店・他要素は一切変更しない。**
2. `git diff` で「変更が STORES の5行（＋本handoff追加）だけ」であることを確認。
3. ブランチ `fix/issue-1-click-positions` を切り commit → push → `gh pr create` で #1 を参照（`Closes #1` は付けない＝目視確認後にボス/監督が close）。

## スコープ外（#3 へ回す・本件で触らない）
読取検証で疑義のある項目（要ボス/振興組合確認）:
- 掲載漏れ疑い: `デイサービスはるの森` / `A daccha`（中央列に名称あり・現データ未収載の可能性）
- ランドマーク扱いか要確認: `遊歩ガーデン` / `絹敷会` / `花察羽`
→ これらは捏造せず Issue #3 でボス確認。本PRでは追加しない。
