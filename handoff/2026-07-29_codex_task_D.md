# Codex 作業指示 D — 座標の差し戻し（C-3の誤りを修正）

対象: `preview.html` と `tools/v2-build/`。**`index.html` / `v2.html` は変更しない。git操作もしない。**
B+C（`handoff/2026-07-29_codex_task_BC.md`）の成果は良好で、位置ズレは直っています。**C-3 の座標再判定だけ2件差し戻します。**

## 前提 — このプロジェクトのデータ源は `nakayaman.com`（なかやま商店街振興組合）に統一されている

`handoff/v2_data_provenance.md` の通り、店名・住所・カテゴリは振興組合の掲載を一次ソースにしています。**各店の自社サイトで住所が食い違っても、勝手に乗り換えないこと。** 食い違いは注記として残し、判断は振興組合に確認する扱いです。

---

## D-1. デイサービス はるの風 — 985m 動かしたのは誤り（最優先）

`38.292133, 140.842529`（中山5-11-3）→ `38.2981118, 140.834226`（中山7-15-10）に変更されていますが、**これは誤りです。**

根拠3点:

1. **公式ページに「仙台市青葉区中山5-11-3」と明記**されている（実際に取得して確認済み）
   https://www.nakayaman.com/post/デイサービスはるの風
2. **紙マップ（`map.png`）** でも5丁目19番エリア（お菜とお酒アイリスと遊季ガーデンの間）に描かれている
3. **あみさんが送ってきたGoogleマップのスクリーンショット**に「ケアステーション はるの風」が Double Egg 4丁目のすぐ近くに写っている

参照された `harunokaze.co.jp` の「中山7-15-10」は、同一法人の別事業所（本部等）の住所とみられます。

**対応**: `lat/lng` を `38.292133, 140.842529` に戻す。遊季ガーデンと同一住所（中山5-11-3）で座標が重なるため、**サトー商会/みなとやと同じ手法で中心から±分離**し、上下は**紙マップの描画順＝はるの風が上・遊季ガーデンが下**とする。両方 `src: approx`。

## D-2. サトー商会 / みなとや — nakayaman.com の住所に戻す

`荒巻本沢1-17-4` → `1-17-14` に変更されていますが、**nakayaman.com の掲載は `1-17-4`** です（実際に取得して確認済み）。https://www.nakayaman.com/post/sato-aramaki

54m の差で実害は小さいものの、上記の前提によりデータ源を勝手に乗り換えないでください。

**対応**: 両店とも `荒巻本沢1-17-4` 由来の座標（元の `38.289295, 140.85054`）に戻し、中心から±分離。上下は紙マップ順（サトー商会が上・みなとやが下）。両方 `src: approx`。
**ただし**「自社サイト `satoh-web.co.jp` では 1-17-14 と記載」を D-3 のメモに**確認事項として残す**こと（振興組合への確認候補）。

## D-3. 記録ファイルを作る（B+Cで作られなかったもの）

`handoff/2026-07-29_coords_recheck.md` を新規作成し、同一座標ペア10店それぞれについて以下を1行ずつ書く:

- 決定した lat/lng と 上下どちらか
- 根拠（どのURL・どの資料の何を見たか）
- `src` の値と、その値にした理由
- 振興組合／あみさんに確認したい点（あれば）

B+C の報告に書いた内容をそのまま移して構いません。D-1・D-2 の差し戻し後の値に更新すること。

---

## 完了報告に含めるもの

1. はるの風・遊季ガーデン・サトー商会・みなとやの最終 `lat/lng/x/y/src` と、上下関係
2. 差し戻し後、あみさん指摘の3点（たけむらや⇔ウエルシアが約25px差／フラワー中山がたけむらやより上／EndRollがcake NAOより上）が**維持されている**ことの実測値
3. `python tools/v2-build/build_mapdata.py --preview` が exit 0 で通ること
4. `git diff --stat -- index.html v2.html` が空であること
